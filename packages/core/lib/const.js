const LOWEST_NODE_VERSION = '12.0.0'

const DEFAULT_CLI_HOME = '.polaris-cli'

const PKG_CONFIG_WITH_COMMAND = {
  init: '@polaris-monorepo-cli/init'
}

module.exports = {
  LOWEST_NODE_VERSION,
  DEFAULT_CLI_HOME,
  PKG_CONFIG_WITH_COMMAND
}
